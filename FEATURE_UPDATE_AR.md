# تحديث Edubia — Details + Availability + Feedback

هذه النسخة جاهزة لرفع محتوياتها مباشرة إلى نفس GitHub repository المرتبط بـVercel.

## قاعدة البيانات القديمة

المشروع ما زال يستخدم نفس مشروع Supabase القديم الموجود في:

`public/config.js`

لا تنشئ قاعدة بيانات جديدة، ولا ترفع قاعدة البيانات إلى GitHub.

- إذا سبق وشغّلت `RUN_ONCE_SAFE_DATABASE_UPGRADE.sql` بنجاح: لا تحتاج لتشغيل SQL جديد لهذا التحديث.
- إذا لم تشغّله من قبل، أو ظهرت أخطاء تخص `feedback` أو `unavailable_slots`: شغّل `RUN_ONCE_SAFE_DATABASE_UPGRADE.sql` مرة واحدة من Supabase SQL Editor.
- لا تشغّل `database.sql` على قاعدة البيانات القديمة، لأنه مخصص فقط لمشروع جديد وفارغ.

## المميزات الجديدة

- زر **Details** واضح بجانب كل طالب.
- نافذة تفاصيل منظمة تشمل البيانات الشخصية، إجمالي الأسبوع والشهر، عدد الجلسات، وعدد سجلات Feedback.
- إضافة وتعديل وحذف الجلسات من نافذة التفاصيل.
- زر **Feedback** واضح بجانب كل طالب.
- إضافة وتعديل وحذف Feedback لكل جلسة.
- درجات من 1 إلى 5 للفهم، الالتزام، التطبيق العملي، حل المشكلات، التمارين، والمشاركة.
- تسجيل الحضور والواجب وما تم شرحه ونقاط القوة ونقاط التحسين.
- فلترة Feedback حسب الجلسة.
- طباعة التقرير أو حفظه PDF من نافذة الطباعة.
- تنزيل Feedback بصيغة JSON.
- خانات الجدول الفارغة تظهر **Available** باللون الأخضر.
- زر **Busy** يحوّل الوقت إلى **Unavailable** باللون الأحمر.
- زر **Available** يعيد فتح الوقت.
- نافذة إضافة الجلسة تمنع اختيار وقت محجوز أو مغلق.
- خط Manrope أوضح مع تحسين أحجام النصوص والمسافات.
- تصميم Responsive للموبايل والكمبيوتر.

## رفع التحديث

1. فك ضغط ZIP.
2. ارفع الملفات الموجودة داخله إلى جذر GitHub repository القديم واستبدل الملفات الحالية.
3. اعمل Commit على فرع `main`.
4. Vercel سيعمل Deployment تلقائيًا إذا كان مرتبطًا بنفس repository.

إعدادات Vercel:

- Framework: Vite
- Build Command: `npm run build`
- Output Directory: `dist`
- Root Directory: `./`
